{% extends 'layout.html.twig' %}

{% block content %}
    <div class="container">
        <h1>
           {{ artwork.title }}
        </h1>
        <img src="{{ artwork.primaryImage }}" alt="image of {{ artwork.title }}">
    </div>
{% endblock %}